# Assets visuais do README

Coloque aqui os **prints em PNG** (ou WebP) referenciados na raiz do `README.md`.

| Arquivo sugerido | O que capturar |
|------------------|----------------|
| `swagger-ui.png` | Swagger UI da API **em produção** (Railway): pelo menos um endpoint expandido + tags visíveis. Largura recomendada ~1200–1400 px. |
| `railway.png` | Dashboard Railway do projeto: serviço da API + plugins (Postgres/RabbitMQ) ou variáveis mascaradas (sem segredos). |
| `rabbitmq-management.png` | Interface de gestão do RabbitMQ (**Queues** ou **Overview**) mostrando filas/exchanges relacionadas ao domínio de agendamentos. |

**Privacidade:** borre hosts internos, tokens, senhas e dados reais de clientes antes do commit.

O diagrama de arquitetura embutido no repositório é o arquivo vetorial `architecture.svg` (pode ser substituído por um export seu do Draw.io ou por um print do diagrama Mermaid do README).
